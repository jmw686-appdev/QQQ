<div class="row mb-3">
  <div class="col-md-12">
    <h1 class="display-4">
      Required Fields

      <%= link_to new_required_field_path, class: "btn btn-lg btn-outline-secondary" do %>
        <i class="fas fa-plus fa-fw"></i>
      <% end %>
    </h1>
  </div>
</div>

<!-- Table example -->

<div class="row">
  <div class="col-md-12">
    <div class="table-responsive">
      <table class="table table-striped">
        <thead>
          <tr>
            <th>
              Input type
            </th>

            <th>
              Instructions
            </th>

            <th>
              Question template
            </th>

            <th>
              Options
            </th>

            <th>
              Actions
            </th>
          </tr>
        </thead>

        <tbody>
          <%= render collection: @required_fields, partial: "table_row", as: :required_field %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<!-- Card deck example -->

<% @required_fields.each_slice(3) do |slice| %>
<div class="row">
  <div class="col-md-12">
    <div class="card-deck">
      <%= render collection: slice, partial: "card", as: :required_field %>
    </div>
  </div>
</div>
<% end %>

<!-- List group example -->

<div class="row justify-content-center mb-3">
  <div class="col-md-8">
    <div class="card">
      <h1 class="card-header h4 d-flex align-items-center justify-content-between">
        Required Fields

        <%= link_to new_required_field_path, class: "btn btn-sm btn-outline-secondary" do %>
          <i class="fas fa-plus fa-fw"></i>
        <% end %>
      </h1>

      <div class="list-group list-group-flush">
        <%= render collection: @required_fields, partial: "list_item", as: :required_field %>
      </div>
    </div>
  </div>
</div>
